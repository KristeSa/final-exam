import { Box } from "@mui/material";
import { NavBar } from "../navbar";

export const Header = () => {
  return (
    <Box role="header" bgcolor="#e0e0e0" padding="20px">
      <NavBar />
    </Box>
  );
};
